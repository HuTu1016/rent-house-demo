spring:
  datasource:
    url: ${APP_DB_URL:jdbc:mysql://localhost:3306/rent_house?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai}
    username: ${MYSQL_USER:rent_house}
    password: ${MYSQL_PASSWORD:rent_house_dev}
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6380}
      password: ${REDIS_PASSWORD:}
  jpa:
    show-sql: true
logging:
  level:
    com.renthouse: DEBUG
