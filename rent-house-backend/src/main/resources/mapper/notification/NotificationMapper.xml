<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.renthouse.notification.mapper.NotificationMapper">

    <select id="countNotifications" resultType="long">
        SELECT COUNT(*) FROM system_notification WHERE recipient_id = #{userId}
    </select>

    <select id="listNotifications" resultType="map">
        SELECT id, notification_type, title, content, read_at, created_at
        FROM system_notification
        WHERE recipient_id = #{userId}
        ORDER BY created_at DESC
        LIMIT #{size} OFFSET #{offset}
    </select>

    <select id="countUnread" resultType="long">
        SELECT COUNT(*) FROM system_notification WHERE recipient_id = #{userId} AND read_at IS NULL
    </select>

    <update id="readNotification">
        UPDATE system_notification SET read_at = #{now} WHERE id = #{id} AND recipient_id = #{userId}
    </update>

    <update id="readAllNotifications">
        UPDATE system_notification SET read_at = #{now} WHERE recipient_id = #{userId} AND read_at IS NULL
    </update>

</mapper>
